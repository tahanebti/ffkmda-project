import { SuggestionHelperPipe } from './suggestion-helper.pipe';

describe('SuggestionHelperPipe', () => {
  it('create an instance', () => {
    const pipe = new SuggestionHelperPipe();
    expect(pipe).toBeTruthy();
  });
});
